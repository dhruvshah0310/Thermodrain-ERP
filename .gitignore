node_modules/
dist/
dist-preview/
build/
*.log
.DS_Store

# Inlined single-file artifact build output — regenerate with
# `npm run build:preview --workspace client` + the inline step; not source.
client/artifact-build/

# SQLite runtime database — generated by seed/import scripts, not source.
server/data/*.db
server/data/*.db-journal
server/data/*.db-wal
server/data/*.db-shm

.env
.env.local
